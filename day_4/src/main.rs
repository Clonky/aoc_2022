use std::collections::HashSet;
use std::fs;

fn read_

fn main() {
    const SRC: &str = "test.txt";
    let contents = fs::read_to_string(SRC).expect("Couldn't read file");
    let lines = contents.lines();
}
